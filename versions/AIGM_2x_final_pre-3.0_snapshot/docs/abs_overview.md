ABS – Abstraction Layer Overview

ABS is a read-only analytics layer.

It:
- reads tier status
- reads merit
- produces rankings and summaries

It does not:
- assign tasks
- grant or remove tiers
- change the system

Think of ABS as a dashboard, not a commander.
